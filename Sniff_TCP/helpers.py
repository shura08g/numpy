def saveSEQ(seq):
    seqFile = open('seq', 'w')
    seqFile.write(str(seq))
    seqFile.close()


def saveACK(ack):
    seqFile = open('ack', 'w')
    seqFile.write(str(ack)
    seqFile.close()